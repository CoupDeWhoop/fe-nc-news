export default function Loading () {
    return (
        <section className="section">
            <img src='/load.gif' alt="loading wheel spinning"/>
            <p>Loading...</p>
        </section>
    )
}